# Upload a file to the gallery

Upload a file to the gallery

## Usage

``` r
file_upload(path, api_key = get_api_key())
```

## Arguments

- path:

  file to be uploaded

- api_key:

  RSpace API key

## Value

Parsed JSON response from the API.
